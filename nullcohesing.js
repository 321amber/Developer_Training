// this is an operator which is used to handle undefined and null values.
//  ?? -> returns the first defined value.

let number = 1;

console.log(number??100); //it will give first defiend value, so number will be the output

let destination;

console.log(destination??"indore");

