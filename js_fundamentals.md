Introduction to javascript:-

Javascript is a language to create webpages alive. dynamic language and it runs automatically when page relodes.
History of javascript:-
javaScript firstly called LiveScript. after evolution it called ECMAScript.

javaScript can run in browsers and also in servers.
browsers has embded VM's to run js.

modern js doesnot require low level access of the memory. i manages it itself.

browser JavaScript can add, remove, or modify html pages, can perform various events, send request to remote servers and download/upload the files.
browser JavaScript can not create servers, cannot access files from local storage.

To create server or access the local storage we use node.js and its modules.

// -> refers single line comment
/* ... */ -> refers multiline comments.

'use strict';
on the top of the page, so that we only modern javascript functionalities can work, old cannot,
we should write the use strict on the top of the page.